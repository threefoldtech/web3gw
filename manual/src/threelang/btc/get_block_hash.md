# Get Block Hash Action

> Returns the hash of the block in the best block chain at the given height.

- action name: !!btc.get.block_hash
- parameters:
  - block_height [required]

## Example

```md
    !!btc.get.block_hash
        block_height: 100
```
